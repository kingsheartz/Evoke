"use client";

/** Reserves vertical space for the fixed site header + announcement + ad strip. */
export function SiteChromeSpacer() {
  return <div className="site-chrome-spacer shrink-0" aria-hidden />;
}
